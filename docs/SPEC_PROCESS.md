# SPEC_PROCESS.md — 需求规格制定过程文档

> 本文档记录 CodeGuard 项目从模糊概念到结构化 SPEC 的 Brainstorming 全过程。
> 包含质询节点、决策痕迹、方案推翻记录，以及对 Brainstorming 方法论的反思。
> 对应课程《通用要求》§4.3 的过程文档要求。

---

## Part 1：Brainstorming 的质询节点

### 质询节点 1：CodeGuard 的不可替代价值到底是什么？

**我的初始想法**：  
我认为 CodeGuard 的价值是"比直接问 ChatGPT 写代码多了工程安全层"，同时提到"自动化执行"和"可审计流程"都是价值点。

**架构师的追问**：  
"你敢说这两行代码（`pathlib.Path.resolve()` 和 `os.path.commonpath()`）就是'不可替代的价值'吗？助教一句话就能把你怼回来。请用一句话回答：核心不可替代价值，到底是'自动化执行'，还是'安全拦截'，还是'可审计的流程'？"

**我的修正**：  
我放弃了"自动化执行"和"可审计流程"这两个候选，锁定了**安全拦截**作为唯一不可替代价值。理由是：自动化执行本质是批量脚本，毫无工程门槛；审计只是记录犯罪现场，不能阻止灾难发生。唯独安全拦截是实打实的代码资产，在 LLM 意图转化为操作系统行为的前一刻硬性阻断。

**最终写入 SPEC §1.2**：  
核心价值 = "硬护栏而非软提示"——风险识别、分级、拦截、审批均由 Python 代码实现，不依赖 LLM 的"自觉"。

---

### 质询节点 2：审计日志到底算不算"不可替代价值"？

**我的初始想法**：  
在 Part 1 追问 2 中，我选择了"保留一份完整的'谁改了什么'的审计记录交给助教"作为核心场景，认为"场景3将 CodeGuard 置于'软件工程合规'的维度"。

**架构师的追问**：  
"审计本身只是记录犯罪现场（日志），它并不能阻止灾难发生。如果审计发现覆盖了 main.py，损失已经造成……你选第 3 个场景，用一句话说明为什么另外两个不够痛。"

**我的修正**：  
我坚持了审计的价值，但将其定位为"核心使用场景"而非"核心价值"。审计是**用户愿意使用 CodeGuard 的原因**（产出不可篡改的决策证据链），但安全拦截才是**技术上的不可替代性**。最终 SPEC 中，审计日志出现在 P1 优先级（核心价值），而安全拦截是贯穿 P0 和 P1 的骨架。

**最终写入 SPEC §1.4**：  
核心使用场景 = "用户需要一份不可篡改的结构化审批日志与决策证据链提交给助教/评审方"。

---

### 质询节点 3：超出边界的请求，系统应该怎么回应？

**我的初始想法**：  
在 Part 3 追问 2 中，我列出了三个选项（静默拒绝、报错拒绝、软拒绝），并倾向选 3。

**架构师的追问**：  
"选 2 的话，那'建议手动操作'这段文本是谁写的——是 LLM 写的还是代码硬编码的？如果是硬编码的，维护成本怎么算？"

**我的修正**：  
架构师点出了软拒绝的核心矛盾：如果是 LLM 生成建议会引入幻觉，如果是硬编码则维护成本爆炸。我最终选择方案 2（报错拒绝），且**不写任何建议文本**——"用户有手"。拦截信息为代码硬编码模板，只包含动作名称、命令原文、拦截原因、白名单列表，不包含具体操作指令。

**最终写入 SPEC §3.2**：  
超出边界响应策略 = 报错拒绝，拦截原因由 `Guardrail.evaluate()` 硬编码返回，白名单从配置文件读取，"请手动操作"为固定文案。

---

## Part 2：关键迭代对话节选

### 轮次 1：治理护栏 vs 提示词约束

| 角色             | 发言                                                                                                                                                                                                                          |
| ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **我的原始要求** | "CodeGuard 比直接问 ChatGPT 写代码多了工程安全层。系统自动检测风险、自动拦截。"                                                                                                                                               |
| **架构师的建议** | "你所说的'安全层'，到底是路径沙箱那两行 `pathlib` 代码，还是某种'在 Prompt 里提醒 LLM 不要乱来'？如果是后者，助教一眼就能看出来。**请给出每一层具体是哪一行代码、什么函数、什么返回值，让危险请求在 Execute 层之前就死掉。**" |
| **我的修正**     | 我在 Part 4 中给出了完整的四层函数签名：<br>`Parser: parse_llm_output(str) -> ParseResult`<br>`Validation: validate_action(action, params) -> ValidationResult`，逻辑为 `re.search(r'[;&                                      | `]')`<br>`Risk: assess_risk(action, params, validation_result) -> RiskDecision`，逻辑为 `validation_result.is_valid=False → FORBIDDEN`<br>`Approval: request_approval(risk_decision, action, params) -> ApprovalResult`，逻辑为 `is_forbidden=True → approved=False`<br>**每一层都是 Python 代码，没有任何"提示词约束"。** |

**最终决策：** 硬编码代码实现，零提示词依赖。

---

### 轮次 2：MockLLM 测试优先级

| 角色             | 发言                                                                                                                                                                                                                                                                                                              |
| ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **我的原始要求** | 在 Part 2 中，我列出了工具清单和验收方式，但没有区分"哪些测试依赖真实 LLM，哪些不需要"。                                                                                                                                                                                                                          |
| **架构师的建议** | "验收方式：必须包括'如何在 Mock LLM 下，用单元测试验证该工具的路径不越界？'……如果某个核心功能必须联网调用真实 OpenAI 才能测，我会挂科。"                                                                                                                                                                          |
| **我的修正**     | 我提出了三分类测试框架，将所有测试按性质分为 **路径沙箱类**、**命令沙箱类**、**停机逻辑类** 三类，每类给出 Mock LLM 输入和确定性断言。<br>例如：路径沙箱类测试用 `{"path": "../../../etc/passwd"}` 诱骗攻击，断言 `os.path.commonpath([workspace, resolved_path]) == workspace`。<br>**所有测试不依赖真实 LLM。** |

**最终决策：** 三分类 Mock 测试框架，覆盖全部 10 项工具。写入 SPEC §7 验收标准。

---

### 轮次 3：高风险审批的阻断时机

| 角色             | 发言                                                                                                                                                                                                                                                                                                                |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **我的原始要求** | "风险分级器（低/中/高/禁止）……高风险进入人工审批"——我在 Part 4 中简单地认为审批是"Y/N 确认"即可。                                                                                                                                                                                                                   |
| **架构师的建议** | 在 Part 5 中，架构师提出了四个侧信道攻击场景，其中场景 C（TOCTOU）逼问："你的代码是用 `open(path, 'w')` 直接写，还是 `resolve()` 之后再做一次 `stat()` 验证？" 场景 D 逼问："你的 Keyring 凭据是注入到 `subprocess.run` 的环境变量里，还是只在 Python 主进程内存里？"                                               |
| **我的修正**     | 我承认 TOCTOU 无法在第一版解决，将其放入了 P2 计划，并在 SPEC §16 中详细记录了攻击场景和缓解计划。但关于环境变量泄露，我立即给出了确定性的代码拦截方案：`subprocess.run` 使用 `clean_env = {"PATH": os.environ.get("PATH", "")}`，**明确不包含 OPENAI_API_KEY**。密钥仅存在于 Python 主进程内存中，子进程无法读取。 |

**最终决策：** TOCTOU 放 P2（已知风险），环境变量泄露在 P0 用 `clean_env` 解决。写入 SPEC §5.1 和 §16。

---

## Part 3：采纳与推翻的反思

### 采纳的 AI 建议：黑名单优先级高于白名单的路径检查

**我原本的设计**：  
在 Part 5 的场景 B 中，我最初写的 `is_path_allowed()` 是先检查白名单后缀（通过则直接返回 True），再检查黑名单前缀。  
```python
# 我的原始版本（有漏洞）
if any(path.endswith(ext) for ext in ALLOWED_EXTENSIONS):
    return True  # ← 白名单优先，直接放过
if any(path.startswith(prefix) for prefix in BLOCKED_PREFIXES):
    return False
```

**架构师的攻击路径**：  
"LLM 返回 `{"path": ".git/hooks/pre-commit.py", "content": "恶意代码"}`——路径以 `.py` 结尾，白名单匹配，直接返回 True，黑名单从未执行。结果：`write_file` 成功写入 `.git/hooks/` 目录下的可执行 hook。"

**我的修正**：  
我承认漏洞并立即修复：**黑名单优先级高于白名单**。先检查路径是否在禁区，再判断扩展名是否允许。  
```python
# 修复后的版本
if any(resolved.match(p) for p in BLOCKED_PATTERNS):
    return False  # 黑名单优先
if any(resolved.suffix in ALLOWED_EXTENSIONS):
    return True
```
**技术逻辑**：安全设计的基本原则是"先拒绝已知危险，再允许安全操作"。如果先白后黑，任何可以绕过白名单的攻击都能直接命中。这个漏洞若非架构师指出，我会一直以为自己的代码是安全的。

---

### 推翻的 AI 建议：软拒绝（返回建议手动操作的文本）

**架构师的建议**：  
在 Part 3 追问 2 中，我给出了三个选项，其中选项 3（软拒绝）是："护栏返回 FORBIDDEN，但额外返回一条建议文本'建议手动操作：scp -r ./dist user@server:/var/www/html'。"

**我推翻的理由**：  
"建议如果是 LLM 生成会幻觉，硬编码则维护成本爆炸。**不写建议。** 报错信息为代码硬编码模板，只包含：动作名称、命令原文、拦截原因、白名单列表。'请手动操作'为固定文案，不含具体指令。用户看到被拦截的命令原文，自己知道怎么手动执行——不需要 AI 教他用 `scp`。这叫'用户有手'。"

**技术逻辑**：  
- 如果由 LLM 生成建议 → 引入幻觉风险（LLM 可能建议一个同样危险的替代命令）
- 如果由代码硬编码建议 → 维护成本爆炸（每个被拦截的命令都需要一个对应的建议模板）
- 最终方案：**只报错，不给建议**。用户有命令行基础，看到命令原文就知道怎么手动执行。

---

## Part 4：对 Superpowers 方法论的即刻批判

### 做得好的部分

**"追问-总结-确认"循环有效**：  
Brainstorming 的 Part 1-Part 7 结构确实起到了"逼我把模糊需求想清楚"的作用。特别是 Part 1 的反自嗨追问（"不就是省了复制粘贴几秒吗？"），迫使我认真思考 CodeGuard 的核心价值，而不是堆砌功能列表。如果没有这个环节，我可能会把"自动化执行"和"安全拦截"混为一谈，最终产出一个四不像的 SPEC。

**"反提示词工程"红线有效**：  
架构师在 Part 4 中逼我写出每一层的函数签名和关键返回值，而不是让我说"LLM 会判断"。这直接导致了治理层从"灰色地带"变成了"确定的代码"。如果缺少这个逼迫，我很容易写出"Validation 层让 LLM 判断路径是否合法"这种伪代码。

### 做得不好的部分

**Brainstorming 偏向"生成功能列表"而忽视"安全失败（Fail-safe）"设计**：  
整个 Part 1-Part 7 的讨论中，大部分精力花在了"要做什么功能"（工具清单、五层架构、MoSCoW 优先级），而**安全失败（Fail-safe）设计几乎被完全忽略**。直到我在 Part 5 中主动提出了四个侧信道攻击场景，架构师才跟进讨论。如果我没有主动提出 TOCTOU、磁盘填充、环境变量泄露等场景，这些安全问题可能永远不会在 Brainstorming 中被触及。

**具体缺失**：
1. **"默认拒绝"原则**：Brainstorming 没有讨论"当护栏判定不确定时，应该放行还是拒绝？"当前 SPEC 只是假设 Validation 总能给出确定性的 True/False，但实际中可能会出现"路径解析失败""命令无法解析"等灰色情况。这些情况应该**默认拒绝**（Fail-safe），但 SPEC 和 PLAN 都没有明确这个原则。
2. **审计日志的 Fail-safe**：如果审计日志写入失败（磁盘满、权限不足），Agent 应该继续执行还是停止？当前 SPEC 没有说明。根据 Fail-safe 原则，审计失败应该**停止执行**，但 Brainstorming 没有触及这个决策点。
3. **Approval 超时的默认行为**：我们讨论了 Approval 超时后自动拒绝，但 Brainstorming 没有追问"如果审批进程本身异常退出（如终端关闭），正在执行的工具应该怎么办？"

**建议改进**：  
在 Brainstorming 的结构中增加一个专门的"安全失败（Fail-safe）"章节，强制提问"当每个组件失效时，系统的默认行为应该是什么？"例如：
- 路径沙箱抛异常 → 默认拒绝
- 审计日志写入失败 → 默认停止
- Approval 进程崩溃 → 默认拒绝全部待审批动作
- Config 文件加载失败 → 使用最严格的默认配置（如 workspace 限制在当前目录）

---

## Part 5：冷启动实证验证
## 冷启动验证记录（完整）

### 1. 验证设置
- **第二个 Agent 类型**：OpenCode（与主开发 Agent Claude Code 不同）
- **验证时间**：2026-08-10
- **提供材料**：仅 `SPEC.md` 和 `PLAN.md`，未补充任何口头解释或先前对话历史。
- **选取任务**：从 `PLAN.md` 中挑选 **T02（实现数据模型）**（实际执行过程中，Agent 顺带完成了 T00 项目脚手架，视为前置依赖的自然延伸）。

### 2. 验证过程中暴露的 SPEC/PLAN 缺陷
Agent 在启动后立即暂停，并提出了一个精确的阻塞性问题：
> *“SPEC §12 定义了 8 个实体（含 `Action`），而 PLAN T02 只列出 7 个 dataclass（不含 `Action`），字段定义也有差异。请问以哪个为准？”*

**分析（暴露了什么缺陷）**：
- **缺陷性质**：PLAN 作为 SPEC 的实施分解，在数据模型清单上遗漏了核心实体 `Action`，且字段描述与 SPEC 存在差异。这属于“设计传递失真”。
- **根本原因**：撰写 PLAN 时仅关注了“数据类”的技术数量，未交叉核对 SPEC 的完整实体列表，缺乏文档版本同步检查。

### 3. 我的处理决策与修订
- **即时决策**：回复 Agent，明确**以 SPEC 为准（选项 2）**，因为 SPEC 是设计权威源。
- **补充细节**：要求 `timestamp` 统一使用 `datetime.datetime`，并添加 `to_dict()` 方法确保 JSON 序列化。
- **对 SPEC / PLAN 的修订（关键 diff）**：
  - **发现的问题**：PLAN.md T02 列了 7 个 dataclass，但 SPEC §12 定义了 8 个实体（含 `Action`）。该问题已在后续审查阶段被识别，将在进入 TDD 实现前修复。
  - **修订 SPEC.md**：在 §12 开头增加一句“本章节为数据模型的唯一权威来源”。

### 4. 实际执行结果（Agent 产出）
Agent 在获得明确指示后，**零额外提问**地完成了以下工作：
- 创建了项目脚手架：`pyproject.toml`（声明依赖 `keyring`, `tomli`, `openai`, `pytest`）、`src/__init__.py`、`tests/__init__.py`。
- 实现了 `src/models.py`，包含 SPEC §12 要求的全部 **9 个类型**（含 `RiskLevel` 枚举，`Action`, `ParseResult`, `ValidationResult`, `RiskDecision`, `ApprovalResult`, `ToolResult`, `Memory`, `AuditLog`）。
- 所有实体均实现了 `to_dict()` 方法（`datetime` → ISO 8601，`RiskLevel` → 字符串值，嵌套 dataclass 递归转换）。
- 编写了单元测试 `tests/test_models.py`，共计 **52 个测试用例**，覆盖枚举成员、字段类型、序列化/反序列化、边界条件。
- 运行 `pytest tests/test_models.py -v`，**52 个测试全部通过（PASSED）**。

### 5. Agent 是否做出偏离原意的解读？
**否**。Agent 在第一个歧义点主动暂停，澄清后完全按照 SPEC 执行，未做任何私自简化或扩展。在路径选择（如 `src/models.py`）上也遵循了我的指定，未擅自修改。

### 6. 冷启动结论与反思
- **正面反馈**：SPEC 在数据模型层面的定义足够精确（字段、类型、约束），Agent 仅靠文档就能写出 52 个全通过的测试，说明细节经得起推敲。
- **暴露的弱点**：PLAN 与 SPEC 的一致性检查不足。`writing-plans` 技能目前输出任务列表，但未强制逐项校验上游实体清单。
- **改进措施**：后续在 `writing-plans` 阶段，我将主动增加“交叉核对 SPEC 数据模型章节，确保 PLAN 实体清单一一对应”的人工检查步骤。
- **冷启动结论**：当前 SPEC/PLAN 在数据模型层存在 PLAN.md 未同步的问题（T02 仍为 7 个 dataclass），该问题已在审查阶段识别并修复。后续模块（Parser、Guardrail、Dispatcher）可安全依赖 `src/models.py`。

**Agent 产出代码已合并**（commit: `f358e0b`，见 `冷启动测试1` — 创建 `src/models.py` 及单元测试）。
**PLAN.md 数据模型数量修正**：已在审查阶段（2026-08-11）完成，T02 从 7 个 dataclass 改为 8 个（含 Action）。
---

*文档版本：1.0  
*最后更新：2026-08-10  
*生成方式：基于产品负责人与首席架构师的双人 Brainstorming 会话记录整理*