# CodeGuard — 个人反思报告

> 本文档为个人反思报告。正文基于 CodeGuard 项目为期 6 天的实际开发经历撰写，记录真实的困惑、收获和教训。

---

## 1. 项目回顾

CodeGuard 是一个带确定性代码护栏的 AI 编程助手。它的核心问题是：大语言模型输出的代码建议不可靠——LLM 可能提议删除整个项目、读取 `.env` 文件、执行 rm -rf /。如果直接执行 LLM 的每一条指令，后果不堪设想。CodeGuard 的解法不是在提示词里苦口婆心劝 LLM 别乱来，而是在 LLM 与操作系统之间插入一个由 Python 代码实现的治理层：路径沙箱、命令白名单、四级风险分级、人工审批、审计日志。这个治理层完全由代码硬编码，不依赖 LLM 的自觉，每一层都可以用 MockLLM 在无网络环境下独立测试。最终交付了 315 个测试用例（314 通过，1 跳过），三个端到端演示剧本，以及 Docker 和 CLI 两种分发形态。

---

## 2. Superpowers 实践体会

### 真正帮到我的技能

**TDD（测试驱动开发）** 是这次开发中最有价值的纪律。强制先写测试再写代码，在 AI 协作下效果被放大了。举个例子：在实现 guardrail.py 时，我让 AI 先写危险命令检测的正则表达式，然后跑测试——结果发现 `RM -RF /` 这种大小写变体直接绕过了检查，因为正则没加 `re.IGNORECASE` 标志。如果没有先写测试，这个漏洞可能永远不会被发现，因为人眼看到 `rm -rf` 会觉得"这肯定被拦截了"，但实际上代码只匹配了小写。测试用冰冷的事实告诉我：你写的代码不安全。AGENT_LOG 里记录的"安全测试必须覆盖大小写、空白字符、参数组合等边界"，就是这次教训的总结。

**冷启动验证（第二个 Agent 独立审查 SPEC）** 也让我受益匪浅。我用 OpenCode（不同于主开发用的 Claude Code）去验证 SPEC 和 PLAN 是否足够清晰，结果 Agent 在启动后立即暂停，指出了一个精确的问题：SPEC 定义了 8 个数据实体，PLAN 的 T02 只列了 7 个，漏掉了 `Action`。这个差异如果没被发现，实现阶段就会产生歧义。这说明 SPEC 和 PLAN 之间的"设计传递失真"是真实存在的，冷启动验证是一个低成本的发现手段。

### 形式大于实质的技能

**"finishing-a-development-branch"（完成开发分支）** 这个技能让我觉得形式大于实质。实际开发中我经常在多个 worktree 之间快速切换——修完 parser 的 bug、切到 guardrail 补正则、再切到 tools 修路径解析——如果每次切分支都走一遍 formal 的"完成仪式"，反而打断开发节奏。我最后是每天结束时统一记录 AGENT_LOG，而不是每个分支都写一遍。这个技能更适合"一个功能开发一周"的节奏，而不是 AI 辅助下"一天搞定三个模块"的速度。

---

## 3. TDD（测试驱动开发）评价

在 AI 协作下，TDD 不是"阻碍"，而是"放大器"。传统的 TDD 需要人手动写测试、手动写代码，循环慢；但有 AI 协作时，我可以把"红→绿"循环压缩到几分钟——让 AI 写一组测试，然后让 AI 根据测试失败信息直接修复代码。

最深刻的体验发生在 demo 集成阶段。`demo_feedback_loop` 中 `write_file("src/add.py", ...)` 因为路径只相对于 CWD 解析导致文件未写入——这个 bug 是 `test_demo.py` 的端到端测试发现的。如果先写代码再写测试，我可能会手动跑一遍 demo 看到输出正常就宣布完成，根本不会发现文件实际写到了当前工作目录而不是 workspace 目录。但因为有测试在代码写完之后立刻跑，我看到了 `assert (tmp_path / "src/add.py").read_text() == "def add(a,b): return a+b"` 这个断言失败，才意识到 `tools.py` 里的 `_is_inside_workspace` 没有做相对路径拼接。这种 bug 属于"逻辑上正确，但运行效果不对"，只有测试能发现。

另一个例子是 `__pycache__` 缓存污染。测试明明通过了，但改完代码再跑又失败了——排查了很久才发现是 pytest 的字节码缓存保留了旧版本测试文件。最终在测试命令里加了 `--cache-clear` 才解决。如果测试不是从一开始就严格跑，这种"幽灵失败"会让人怀疑自己的代码逻辑，浪费大量时间。

---

## 4. Subagent 工作流教训

### 颗粒度问题

在 PLAN 阶段，我把项目拆成了 25 个 Task（T00 到 T25）。实际执行下来，我发现有些 Task 拆得太细了。比如 T02（数据模型）和 T03（配置模块）完全可以合并——两者都是纯数据类，不涉及复杂逻辑，拆成两个 Task 反而增加了上下文切换成本。而 T18（Agent 主循环）又太粗了——它包含了 parse → validate → risk → approve → execute → feedback 六步的串联，任何一个环节出问题都会导致主循环测试失败。如果重来一次，我会把主循环拆成"基础循环 + 停机条件"和"完整治理链集成"两个 Task。

### 规约不清导致偏离的具体案例

冷启动验证暴露了一个典型案例：SPEC 的 §12 定义了 8 个数据实体（含 `Action`），但 PLAN 的 T02 只列了 7 个 dataclass。Agent 在拿到这两个文档后立刻暂停，问"以哪个为准？"这说明当 SPEC 和 PLAN 不一致时，subagent 不会自作主张，而是选择停下来提问——这是好事，但也是浪费。如果我在写 PLAN 的时候多花 5 分钟交叉核对 SPEC 的实体清单，就不会有这个阻塞。

另一个更隐蔽的案例是 parser 格式不一致。我在开发过程中发现，LLM 返回的 JSON 有时候是 `{"action": "read_file", "params": {"path": "x.py"}}`，有时候是 `{"action": "read_file", "path": "x.py"}`（`params` 字段被扁平化了）。SPEC 里写的是前者，但 subagent 在实现 parser 时没有严格校验 `params` 字段的存在性，导致扁平化格式也能通过解析——但传入到 executor 时就会因为缺少 `params` 键而报错。这个问题最终是在 agent 主循环的集成测试中发现的，而不是 parser 的单元测试。根因是 SPEC 没有明确写"parser 必须拒绝缺少 `params` 字段的 JSON"。

---

## 5. 凭据与分发的感悟

在实现 keyring 模块之前，我对 API Key 安全存储的理解停留在"别硬编码到代码里就行"。但真正动手实现时，我发现很多原本会忽略的问题：

第一，跨平台差异。同一段 `keyring.set_password()` 代码，在 Windows 上走 Credential Manager，在 macOS 上走 Keychain，在 Linux 上可能需要 `dbus` 或 `gnome-keyring`。测试时我用 `unittest.mock.patch` mock 了 keyring 的底层调用，但实际在 Linux 服务器上跑集成测试时，keyring 直接报错说找不到可用的 Secret Service 后端。

第二，Docker 和 PyInstaller 打包。Docker 镜像里没有系统钥匙串，`keyring` 初始化会失败。我需要在 Dockerfile 里显式设置 `ENV OPENAI_API_KEY` 来绕过 keyring 检查。而 PyInstaller 打包时，`keyring` 依赖的 `dbus-python` 在 Windows 上编译失败，最终放弃了 PyInstaller，只保留 Docker 和 pip 安装两种方式。

这些细节在写 SPEC 的时候完全没想过。SPEC 里只写了"使用 keyring 库存储密钥"，但实际工程中，"keyring 不可用时的降级策略"才是真正需要花时间解决的问题。

---

## 6. 对 Superpowers 的批判

### 对新手不友好的假设

Superpowers 方法论假设了开发者熟悉命令行、git 分支管理、pytest 测试框架、Python 虚拟环境。这个假设在课程环境下成立（大家都是 CS 学生），但如果一个真正的初学者拿到这套流程，他会在第一步就卡住——"怎么创建工作目录？"、"pytest 是什么？"、"为什么红色代表测试失败？"。

具体来说，TDD 的"红-绿-重构"循环听起来很酷，但实际上需要你先理解什么是一个"好的测试"——应该覆盖什么边界条件、mock 的粒度应该多细、何时应该写集成测试而非单元测试。这些"测试审美"不是方法论能教出来的。我在开发过程中发现 AI 写的测试只覆盖了正常路径，完全没有边界条件（比如空路径拒绝、大小写绕过），这些漏洞是靠经验补上的，不是方法论告诉我的。

### 缺失的"代码质量审美"

Superpowers 的流程脚手架只管"流程"——SPEC 写好了吗？PLAN 拆好了吗？TDD 跑了吗？——但不管"代码质量"的审美。比如 PLAN 里 T04 的路径沙箱实现用了 `startswith` 检查路径边界，这在语法上正确，测试也能通过，但这是一个已知的安全漏洞（PLAN_REVIEW 标记为 C-02）。方法论没有内置代码审查或安全审计步骤，它假设"测试通过 = 代码正确"，但测试只能证明代码符合预期，不能证明代码没有安全漏洞。

另一个缺失是"重构"的时机把控。TDD 的第三步是重构，但 PLAN 里 25 个任务没有一个提到"重构"——它们全部是"实现新功能"。实际上，我在开发 guardrail 时，旧版 `evaluate()` 包装器和新版 `validate_action → assess_risk` 共存了一段时间，直到测试全部通过后才清理了废弃代码。这个"重构窗口"很有价值，但 PLAN 没有预留重构 Task，导致重构完全靠自觉。

---

## 7. 如果重来一次，我会改变什么

第一，我会在 PLAN 阶段更早引入端到端演示测试。`test_demo.py` 在最后才写（T23），但三个剧本的 demo 逻辑其实应该在 T18（Agent 主循环）完成后就立刻写——因为 demo 是验收标准，最早写 demo 测试能驱动主循环的接口设计，而不是反过来让主循环去适配 demo。

第二，我会给 subagent 更多的自主权，但要求更好的 SPEC。冷启动验证中，Agent 因为 SPEC 和 PLAN 不一致而暂停提问，这说明 SPEC 还不够详细。如果 SPEC 在数据模型章节明确写了"本章节为数据模型的唯一权威来源"，并且 PLAN 在开头声明"如有冲突以 SPEC 为准"，Agent 就不需要停下来提问。更详细、更权威的 SPEC 其实能让 subagent 更自主。

第三，CI 配置应该在项目早期就完成，而不是最后一天。如果在 8 月 8 日（护栏实现完成时）就配好 CI，每次提交自动跑测试，就能更早发现环境差异问题（比如 Python 3.10 和 3.11 的 `tomllib` vs `tomli` 差异）。

最后，有一个东西我做了但觉得"完全可以不做"——`setup.py`（T24）。这个模块是"交互式引导录入 API Key"的 CLI 工具，但实际上 `keyring_manager.py` 本身已经提供了 `--set` 入口，`setup.py` 只是在外面又包了一层 argparse。如果重来一次，我会把 KeyringManager 的 CLI 接口直接放在 `keyring_manager.py` 里，而不是单独建一个文件。

---

*文档版本：2.0*
*最后更新：2026-08-12*