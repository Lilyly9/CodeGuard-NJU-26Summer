# CodeGuard — 个人反思报告

> 本文档为个人反思报告骨架。AI 已生成大纲和提示问题，**正文由你本人撰写**（1500-2500 字）。
> 每个小节下用 `<!-- 提示问题 -->` 标注引导性问题，帮助你组织思路。

---

## 1. Superpowers 技能评价

<!-- 提示问题：
- 哪些技能发挥了最大作用？（如：TDD 强制执行让你不敢跳过测试、Plan 拆分让复杂任务可批量委托给 subagent）
- 哪些技能"形式大于实质"？（例如：某些子 agent 任务拆分过细，subagent 之间的上下文传递成本超过了独立执行收益）
- 有没有某个技能你用了之后觉得"这其实是多余的"？
-->

*（在此撰写你的回答）*

---

## 2. TDD 在 AI 协作下的体验

<!-- 提示问题：
- 严格"红-绿-重构"在你的项目里是阻碍还是放大器？（例如：先写测试迫使你想清楚了接口签名，减少了后期重构；但某些探索性功能写测试反而拖慢了节奏）
- 与传统的"人写代码、人写测试"相比，AI 协作下的 TDD 有什么不同？（例如：AI 擅长根据错误信息快速修复，让"红→绿"循环极快）
- 你在哪个模块最深刻地感受到了 TDD 的价值（或痛苦）？
-->

*（在此撰写你的回答）*

---

## 3. Subagent 自主运行极限

<!-- 提示问题：
- 智能体（subagent）最长能自主运行多久而不偏离主题？（例如：约 2 小时，之后需要人工重述目标或用新 prompt 重新校准）
- 最优 Task 颗粒度（按你 PLAN 的实践）是多大？一个 Task 是"实现整个 guardrail.py"还是"实现 guardrail 的 forbidden 分支"？
- 你是如何发现 subagent 偏离了目标？是看输出、看代码、还是看测试结果？
-->

*（在此撰写你的回答）*

---

## 4. SPEC/PLAN 质量对 Subagent 执行的影响

<!-- 提示问题：
- 举一个"规约不清导致 subagent 偏离"的具体案例。（例如：SPEC 未明确相对路径的解析基准，导致 `write_file("src/add.py")` 写到了 CWD 而非 workspace，demo 测试失败）
- 在哪些地方 SPEC 和 PLAN 存在不一致，导致 subagent 需要停下来提问或做出了错误决策？
- 如果 SPEC 再写一次，你会在哪一节增加什么细节？
-->

### 工程教训：`__pycache__` 缓存污染

在验证反馈闭环时，遇到了一个极具隐蔽性的问题：即使代码逻辑完全正确，演示依然失败。排查后发现是 pytest 的字节码缓存（`__pycache__`）保留了旧版本的测试文件，导致第二次运行测试时依然读取了旧的断言。这个教训说明，在自动化测试环境中，缓存污染是比逻辑错误更难发现的敌人。最终通过在测试命令中强制加入 `--cache-clear` 参数彻底解决。这也验证了"先验证再宣称完成"这条纪律的必要性——如果只看代码不看实际运行结果，这个问题会被完全忽略。

*（在此撰写你的回答）*

---

## 5. 凭据管理与分发机制的认知刷新

<!-- 提示问题：
- keyring 替代 .env 文件存储 API Key 这个设计，让你想清楚了哪些原本会忽略的工程问题？（例如：跨平台密钥存储差异——Windows Credential Manager vs macOS Keychain vs Linux Secret Service；用户首次运行时的引导流程必须足够清晰）
- `pyproject.toml` + `pip install -e .` 的可分发设计，让你对"项目交付"的理解有什么变化？（例如：以前只需要代码能跑，现在需要考虑别人怎么装）
- 如果项目要真正发布给 100 个用户使用，你还需要补齐什么？
-->

*（在此撰写你的回答）*

---

## 6. 对 Superpowers 方法论的批判

<!-- 提示问题：
- Superpowers 方法论假设了什么？（例如：假设每个任务都可被原子化拆解、假设 subagent 能独立完成原子任务而不需要全局上下文）
- 这些假设在你的项目里成立吗？（例如：在调试 agent.py 主循环时，需要整体思考"解析 → 验证 → 风险评估 → 审批 → 执行 → 反馈"的闭环，原子化拆解反而破坏了理解）
- 哪些环节你觉得"应该人来主导，AI 辅助"，而不是"AI 主导，人审核"？
- 冷启动测试（第二个 Agent 验证 SPEC）暴露了什么问题？这个方法论本身是否过度依赖"第一个 Agent 写的 SPEC 质量"？
-->

*（在此撰写你的回答）*

---

## 7. 如果重来一次，我会改变什么

<!-- 提示问题：
- 你会在 PLAN 阶段更早引入哪些东西？（例如：端到端演示测试 `test_demo.py` 作为验收驱动，而不是最后才补）
- 哪些模块的 TDD 顺序应该调整？（例如：应该先写 agent.py 的集成测试再写单元测试，因为主循环的接口签名是在集成测试中才稳定下来的）
- 你会给 subagent 更多还是更少的自主权？
- 你的 SPEC 会写得更详细还是更简洁？
- 有什么是你做了但觉得"根本不需要做"的？
-->

*（在此撰写你的回答）*

---

*文档版本：1.0*
*最后更新：2026-08-12*
*正文待填写*